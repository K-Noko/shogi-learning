# shogi-learning プロジェクト指針

## プロジェクトの位置づけ

一次目的は「Webフルスタック開発技術の体得」であり、完成したアプリは副産物。

開発は決定ポイント（後戻りコストが高い／目的に沿った学びがある／どちらが優れているか自明でない、のうち2つ以上を満たす分岐点）で必ず立ち止まり、選択肢と得失を提示してユーザーの判断を仰いでから進めること。積み上げ方針は垂直スライス（Walking Skeleton）を基本とする。

**仕様・設計の判断をAIに委ねきらない**。決定ポイントの基準を1つ以下しか満たさない細かい判断であっても、仕様として意味のある選択（データ構造・アルゴリズムの選び方・エッジケースの扱い等）は暗黙のうちに決めて実装を進めない。判断が必要な点は明示的に提示し、ユーザーが自分で考えた上で選択するか、提案を明示的にAccept（「OK」「それで進めて」等）してから実装に入ること。提示せずに実装だけが先行する状態（バイブコーディング）を避ける。

**理解のギャップを埋めることを、先へ進むことより優先する**。先回りした的確な情報提供で、スムーズに実装までたどり着くことを成果とみなさない。論点を示すときは、選択肢と得失より先に「その論点がなぜ生まれるか」を、このプロジェクトの具体的な場面で示す。ユーザーが「なぜそれを思いついたのか」を問うたら、先へ進まず説明する。理由を理解しないまま提案をAcceptする状態は、提示せずに実装が先行する状態と同じく、判断をAIに委ねきっている。

**既存の実装を「既に決定されたアーキテクチャ」として前提にしない**。あるコミット・PRの構造（クラス分割、メソッドの持ち方、分岐の位置等）は、その時点で解決すべきだった別の課題を満たすための手段に過ぎないことがある。既存実装を根拠に「前の設計と矛盾している」と指摘したり、それに倣って実装を進めたりする前に、その構造を導入した経緯（コミットメッセージ、関連Issue）まで遡り、本当に決定事項なのか、暫定実装なのかを確認すること。目的は維持しつつ手段だけを置き換える変更は、前の決定を覆すものではなく正当な発展として扱う。

**過去の実装を前例として引くときは、実装を確かめてから引く**。このリポジトリや、参考にしている他のプロジェクトの実装を「前例がある形」として示すときは、型やフィールドの名前だけで判断せず、それを誰が作り、誰が使っていたかまで実装で確認する。確かめていない前例を付けると、筋の悪い案にも根拠があるように見えてしまう。ただし、筋が悪そうに見える案を、先回りして候補から外すことはしない。どの案を採るかを判断するのはユーザーであり、案を検討して外した経緯にも、設計の理由を書き残せるという意味がある。

**インフラ（永続化・認証・複数セッション管理等）は必然性で導入する**。「複雑さを避けるために先送りする」のでも「学習のために前倒しする」のでもなく、今のアーキテクチャが実際にそれを必要とするタイミングで導入する。

## 開発の進め方: 「動くもの」まで最速、負債は記録して積む

要件はしっかり組むが、「動くもの」までは最速でたどり着く。今すぐ解決しなくてよい課題は意識的に留保してよい。

- 留保してよいのは、構造・非機能面（モジュール分割、DI化、共通フックへの切り出し等）と、機能のスコープの切り詰め
- 留保すると決めた課題は、その場で`debt`ラベルのIssueとして起票し、本文に発生元のIssue番号を書く
- 作業中のPR本文に「## 留保した負債」の見出しを設け、`#N: 〇〇`の形で列挙する
- 妥協した実装のコード側にも、docstringかコメントで「暫定実装（#Nで解消予定）」と残す。後から読んだ人（将来のセッションを含む）が、暫定実装を決定済みの設計と誤読しないようにするため
- **後のPhaseで入ることが確実でも、今のPhaseで使わないフィールドや型の種類は、仕組みごと留保する**。判定の問いは「今から持つと、今のPhaseに新しい判断（振る舞い・検証・テスト）が生まれるか」。確実に要ることは、先に作る理由にならない（YAGNI。`docs/references/design-principles.md`の「いつ決めるか」）。ただし、APIの契約や依存の向きのように後から変えにくいものは、早めに決める
- **テストケースの設計は留保しない**。負債を積むときほど、後のリファクタの安全網になる振る舞いのテストを今書いておく。テストのない妥協実装は、後から「動作を変えずに直す」ことができず、返せない負債になる

## テスト設計の分担

- **何をテストすべきか（ケース一覧・保証内容・防ぐべき失敗シナリオ）はユーザーが指定する**。Claudeはケースを勝手に決めて実装しない。提案はしてよいが、ユーザーのAcceptを経てから書く
- **Vitestの書き方（記法パターン、モック戦略、describe/itの構成等）は、最初の数本をユーザー自身が書いて型を確立する**。Claudeはそれ以降、確立されたパターンに従う
- 将棋のルールの大部分（駒種ごとの移動可能マス・二歩・打ち歩詰め・行き所のない駒への打ち）は、pytestのparametrizeで境界値を列挙してテストする。プロパティベーステスト（Hypothesis）は、詰み判定（王手放置・王手回避）のように列挙では正しさを担保しづらい箇所に限って検討する

## アーキテクチャの前提

- Python（FastAPI）バックエンド × TypeScript（React + Vite）フロントエンド。将来Unityクライアントを追加する可能性がある
- **ルールと状態の真実の源は常にバックエンド**。フロントエンドはサーバーが配信する状態を描画し、ユーザーの意図をPOSTで送るだけの薄いクライアントに徹する。フロントエンドにルールロジックを持たせない（Unityクライアントを同じAPIの別クライアントとして追加できる状態を保つため）
- **フロントエンドとバックエンドの境界では、両側でデータを検証する**。バックエンドはPydantic、フロントエンドはZod（等）で、受け取ったJSONを境界でパースする。TypeScriptの型へのキャストだけで済ませない。tRPCのように、型を共有して境界そのものをなくす構成は採らない
- 形の分岐があるリクエスト（着手の「移動」と「打つ」等）は、Pydanticの判別可能なUnionでモデリングし、「JSONとしては妥当だが意味的には壊れている」リクエストを型で弾く

## 規則の守らせ方

- **機械で検査できる規則は、CIで守らせる**（リンター・型チェッカー・import-linter・テスト等）。文章の規則は読まれなければ効かないが、機械の検査は読まれなくても効く
- 文章（CLAUDE.md・設計書）は予防、CIは保証という役割の違いがある。CIで守らせる規則も、最初から正しく書くために文章で持ってよい。一方、機械で検査できない規則は、文章でしか守れない。どこに書くかは、いつ読まれるべきかで決める（常に守る規則はCLAUDE.md、決定の中身と理由は`docs/design/`）

## 設計書とドキュメント

- 設計判断は`docs/design/`に領域ごとにある（入口は`docs/design/overview.md`）。実装やIssueの下書きの前に、関係するファイルを読む
- 設計書の本体には、決定と、実装中の判断に効く短い理由だけを書く。検討して外した案・経緯・長い論証は`docs/design/rationale/`の同じ名前のファイルに、同じ見出しで置く。決定の文言だけでは実装が自明に決まらないときに読む
- 設計の議論で決まったことは、長い議論の後に文脈が失われないよう、決まった時点で設計書へ反映する。採った案だけでなく、検討して外した案とその理由も`rationale/`に残す
- 判断の拠り所にした一般的な考え方の調べもの（出典付き）は、`docs/references/`にある
- このファイルの規則をどの仕組み（CI・GitHub・Hook・スキル・文章）で守らせるかの棚卸は、`docs/rules/rule-enforcement.md`にある

## ディレクトリ構成・起動・テスト

Walking Skeletonを作った時点で追記する。

## コーディング規約

- **すべての`def`（関数・メソッド）にdocstringを付ける**。1行で用途が明らかな場合でも省略しない
- **テスト関数のdocstringには、何を保証しているかに加えて、何を防ぐテストかも明記する**（保証内容と、その背後にある失敗シナリオの両方を書く）
- **拡張の余地があるクラスには、そのクラスが引き受けないことをdocstringに書き、それが次のどちらかを明記する**。コードだけを読むと、「やっていない」が「やらないと決めた」のか「まだやっていない」のか区別できないため
  - 恒久の境界: 将来も引き受けない。代わりにどこが持つかも書く（例: 盤は盤面データの保持と読み書きに責務を閉じ、千日手や合法手の判定はルールのモジュールが持つ）
  - Phaseの留保: 今のPhaseではやらず、どのPhaseで入るかを書く（例: Phase1のGameSessionは終局を扱わない。Phase2で入る）
- **複雑性が増してきたコードには、非自明な意図・前提をコメントで残す**（処理の順序に依存する正しさ、トリッキーなアルゴリズム上の性質、あえてそうしている理由）。WHATではなくWHYを書く
- コミュニケーション・docstring・コメントは日本語で書く

## Git運用

- 直接`main`にマージしない。作業は必ずfeatureブランチを切り、PRを作成する（マージはユーザー自身が行う）
- コミット前にテスト・型チェックを通すこと
- **PRタイトルの先頭に、解決方法の種類を表す接頭辞を付ける**（`feat:`・`fix:`・`refactor:`・`test:`・`docs:`・`chore:`）
- PR本文の見出しは日本語で書く（例:「## 概要」「## テスト計画」「## 留保した負債」）
- 対応するIssueがある変更は、PR本文にクローズキーワード（`Closes #N`）を含める
- IssueとPRはGitHub上で番号を共有する。複数のIssueを連番で起票したい場合は、関連するIssueをすべて起票し終えてからPRを作成する

## Issue運用

- 着手前に、要件・設計判断・受け入れ条件を詰めてから起票する。実装を進める中で前提が変わったら、起票済みのIssue本文も追従させる

### ラベル

Issueのラベルは、そのIssueが生まれた理由を表す。起票時に決め、解決方法がわかっても付け替えない（解決方法の種類はPRタイトルの接頭辞で表す）。次の3つだけを使い、どのIssueにも必ず1つだけ付ける。

- `enhancement`: ロードマップから計画して積んだもの。アプリの機能だけでなく、開発基盤（Docker構成、テスト基盤、ログ、デバッグ用機能等）も含む
- `bug`: 壊れているのを見つけたもの。別のIssueの作業中に見つかった場合でも、壊れているならこちら
- `debt`: 別のIssueから派生したもの。作業中に意識して留保したものも、終わった後に見つかった穴も含む。本文に発生元のIssue番号を必ず書く

重複・対応しない等の閉じる理由は、ラベルではなくGitHub標準のクローズ理由（completed / not planned / duplicate）で表す。

## Claudeが実行してよいコマンド

**Claudeは、終了せずに持続するプロセスを起動するコマンドを実行しない**。開発サーバー（`npm run dev`、`uv run uvicorn ...`等）も、watchモード（`vitest`をwatchで起動、`tsc --watch`等）も含む。理由：このハーネスのWindows上のバックグラウンド実行は、停止を報告しても子プロセス（`python.exe`・`node.exe`の実体）が生き残り、ポートを握ったままになることがある。古いプロセスがリクエストを処理し続けると、コードの変更が反映されていないように見える。

- 唯一の例外は`docker compose up -d`と`docker compose down`。持続するプロセスをハーネスではなくDockerが管理し、`down`で確実に止まるため。`-d`を付けずにフォアグラウンドで起動しない
- テスト・型チェックは、1回実行して終了する形で実行する（例: `vitest run`、`docker compose run --rm ...`）
- フロントエンドの開発サーバーが必要な確認（ブラウザでの動作確認等）は、ユーザーが行う。Claudeは確認してほしい手順と観点を伝える
